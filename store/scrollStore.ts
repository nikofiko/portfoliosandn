// Shared mutable state for scroll progress and mouse position.
// Written by GSAP ScrollTrigger / mouse handlers, read by R3F useFrame.
export const scrollStore = {
  progress: 0,
  mouseX: 0,
  mouseY: 0,
};
